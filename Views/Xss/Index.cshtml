@{
    ViewData["Title"] = "XSS Senaryoları";
}

<h2>Cross-Site Scripting (XSS) Lab</h2>
<p>Test etmek istediğiniz senaryonun zafiyetli veya korumalı versiyonunu seçin:</p>

<div class="row mt-4">
    <!-- Reflected XSS Kartı -->
    <div class="col-md-6 mb-4">
        <div class="card shadow-sm">
            <div class="card-header text-dark fw-bold">
                Reflected XSS
            </div>
            <div class="card-body">
                <p>Kullanıcıdan alınan veri doğrudan ekrana yansıtılır.</p>
                <div class="d-grid gap-2">
                    <a href="/Xss/ReflectedVulnerable" class="btn btn-dark btn-outline-danger">Zafiyetli Versiyon (Html.Raw)</a>
                    <a href="/Xss/ReflectedSecure" class="btn btn-dark btn-outline-primary">Güvenli Versiyon (Output Encoding)</a>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Stored XSS Kartı -->
    <div class="col-md-6 mb-4">
        <div class="card shadow-sm">
            <div class="card-header text-dark fw-bold">
                Stored XSS
            </div>
            <div class="card-body">
                <p>Veri veritabanına (listeye) kaydedilir ve sayfaya giren herkese yansıtılır.</p>
                <div class="d-grid gap-2">
                    <a href="/Xss/StoredVulnerable" class="btn btn-dark btn-outline-danger">Zafiyetli Versiyon (Html.Raw)</a>
                    <a href="/Xss/StoredSecure" class="btn btn-dark btn-outline-primary">Güvenli Versiyon (Output Encoding)</a>
                </div>
            </div>
        </div>
    </div>
</div>